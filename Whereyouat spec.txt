WhereYouAt Spec

App users:
GET /users
GET /users/:id
POST /users
PUT /users
DELETE /users

Events list:
GET /events
GET /events/:id
POST /events
PUT /events
DELETE /events

Room(rooms will be tied to events):
GET /rooms
GET /rooms/:id
GET /rooms/event     only pull rooms tied to the event you wan(handle with get params instead?)
POST /rooms
PUT /rooms
DELETE /rooms


Event notes: allow users to create events but highlight featured events so UI is not cluttered by bad users.
Featured on front page, events list with a priority based on # of rooms at event in a tab.

https://alligator.io/vuejs/vue-autocomplete-component/
